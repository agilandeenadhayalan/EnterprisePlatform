[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "test-data-generator"
version = "0.1.0"
description = "Generates synthetic ride, location, and payment events for testing"
requires-python = ">=3.11"
dependencies = ["mobility-common"]

[tool.hatch.build.targets.wheel]
packages = ["src"]
