[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "model-ab-test-service"
version = "0.1.0"
description = "A/B test and shadow deployment routing for ML models"
requires-python = ">=3.11"
dependencies = ["mobility-common"]

[tool.hatch.build.targets.wheel]
packages = ["src"]
