[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "reporting-service"
version = "0.1.0"
description = "Scheduled and ad-hoc report generation for ride analytics"
requires-python = ">=3.11"
dependencies = ["mobility-common"]

[tool.hatch.build.targets.wheel]
packages = ["src"]
