[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "schema-migration-service"
version = "0.1.0"
description = "ClickHouse DDL versioning — like Alembic for ClickHouse"
requires-python = ">=3.11"
dependencies = ["mobility-common"]

[tool.hatch.build.targets.wheel]
packages = ["src"]
