apiVersion: v1
kind: Service
metadata:
  name: {{ include "mobility-platform.fullname" . }}
  labels:
    {{- include "mobility-platform.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "mobility-platform.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
