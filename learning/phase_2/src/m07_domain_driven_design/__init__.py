"""
Module 07: Domain-Driven Design
================================

Learn how DDD structures complex business domains into bounded contexts,
aggregates, entities, and value objects to manage complexity at scale.

Key concepts:
- Bounded Contexts (separate models per subdomain)
- Context Mapping (translating between contexts)
- Aggregates (consistency boundary with root entity)
- Value Objects (immutable, identity-less domain objects)
- Invariant enforcement (business rules inside aggregates)
"""
